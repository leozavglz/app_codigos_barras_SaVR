<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo del popup -->
    <solid android:color="@android:color/white"/>

    <!-- Bordes redondeados -->
    <corners android:radius="16dp"/>

    <!-- Padding opcional para crear espacio alrededor del contenido del popup -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp"/>
</shape>
